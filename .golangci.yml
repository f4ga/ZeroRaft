linters:
  enable:
    - govet
    - staticcheck
    - gosimple
    - unused
    - errcheck

    - ineffassign
    - typecheck
    - gofmt
    - misspell
    - gocritic

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gocritic
